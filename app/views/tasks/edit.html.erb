
<%= form_for(@task) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :details %>
  <%= f.text_field :details %>

  <p style="display:inline">Completed? </p><%= f.check_box :completed%>

  <%= f.submit %>
<% end %>


<%= link_to "Back to List", tasks_path %>
